# Models 구역 제약 조건

## 1. 텐서 형태(Shape) 명시 강제
* 모델 설계 시, 데이터 전처리 컨텍스트는 배제하고 오직 수학적 차원(Dimension) 연산에 집중하십시오.
* PyTorch 모듈의 모든 `forward` 메서드 상단 및 주요 연산 과정에 입력/출력 텐서의 형태(Shape)를 주석으로 반드시 명시하십시오. (예: `# Input: x (N, d_in) -> Output: (N, d_out)`)

## 2. 수학적 아키텍처 강제
* HGT 레이어: 노드 타입과 엣지 타입에 따른 독립적인 파라미터 격리 투영 로직을 반드시 포함하십시오.
* KGAT 레이어: Bi-Interaction(이중 상호작용) 기반 층별 업데이트 시, 덧셈(+)과 내적(Element-wise product, $\odot$) 요소가 모두 융합되도록 수식을 구현하십시오.
* 코드가 300줄을 초과하지 않도록 각 GNN 메커니즘을 캡슐화하십시오.
